{
    "build": {
      "nixpacks": {
        "config": {
          "install": "cat requirements.txt && pip install --upgrade pip && pip install --only-binary=:all: -r requirements.txt",
          "pythonVersion": "3.12"
        }
      }
    }
  }